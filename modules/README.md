THESE SCRIPTS NEED TO BE EXECUTED FROM the following directory:

/Desktop/corundum_multicast/modules

**After you do _multicast_revert.sh_ you need to bring the interfaces up again (sudo ip link set IFACE up), and add the IP addresses then (sudo ip addr add IP dev IFACE) manually.**
